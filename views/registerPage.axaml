<UserControl
    x:Class="projectManager.views.registerPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="10">
        <TextBlock
            HorizontalAlignment="Center"
            FontSize="24"
            Text="Register" />
        <TextBox
            x:Name="UsernameBox"
            Width="200"
            Watermark="Username" />
        <TextBox
            x:Name="PasswordBox"
            Width="200"
            Watermark="Password" />
        <TextBox
            x:Name="ConfirmBox"
            Width="200"
            Watermark="Confirm Password" />
        <Button
            Width="200"
            Click="RegisterButton_Click"
            Content="Register" />
    </StackPanel>
</UserControl>
